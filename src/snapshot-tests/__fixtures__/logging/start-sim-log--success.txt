
📝 Start Log Capture

   Simulator: <UUID>
   Bundle ID: io.sentry.calculatorapp

✅ Log capture started.

  ├ Session ID: <UUID>
  └ Filter: Only structured logs from the app subsystem are being captured.

Next steps:
1. Stop capture and retrieve logs: stop_sim_log_cap({ logSessionId: "<UUID>" })
