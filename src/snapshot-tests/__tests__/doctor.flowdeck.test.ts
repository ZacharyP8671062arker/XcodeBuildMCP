import { describe, it } from 'vitest';

describe('doctor workflow (flowdeck)', () => {
  // flowdeck doesn't have a doctor/diagnostics command
  it.skip('no flowdeck equivalent for doctor workflow', () => {});
});
