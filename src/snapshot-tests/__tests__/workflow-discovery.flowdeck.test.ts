import { describe, it } from 'vitest';

describe('workflow-discovery workflow (flowdeck)', () => {
  // flowdeck doesn't have a workflow discovery/management system
  it.skip('no flowdeck equivalent for workflow discovery', () => {});
});
