import { describe, it } from 'vitest';

describe('xcode-ide workflow (flowdeck)', () => {
  // flowdeck doesn't have Xcode IDE bridge tools
  it.skip('no flowdeck equivalent for xcode-ide workflow', () => {});
});
