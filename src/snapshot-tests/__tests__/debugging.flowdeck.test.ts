import { describe, it } from 'vitest';

describe('debugging workflow (flowdeck)', () => {
  // flowdeck doesn't have debugging tools (attach, detach, breakpoints, lldb, stack, variables)
  it.skip('no flowdeck equivalent for debugging workflow', () => {});
});
